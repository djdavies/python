#!/usr/bin/env python
def stars():
	m = int(raw_input("Enter number length "))
	n = int(raw_input("Enter number width "))
	asterisk_function(m, n)
#	for i in range(0, m): # rows
#		for i in range(0, n): # columns
#	   		print "*",
#	  		print
